# db/seeds.rb is intentionally minimal. New games run via `bin/play` (or
# `bin/play --reset`) and go through worldgen + interactive character creation.
#
# Seed something here only if you want a deterministic dev-state independent
# of bin/play's flow.
